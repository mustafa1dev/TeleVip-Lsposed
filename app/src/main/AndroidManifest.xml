<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:description="@string/app_description"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:ignore="UnusedAttribute">

        <!--
          Legacy Xposed descriptor (module API 93). Frameworks that do not understand
          META-INF/xposed/module.prop load com.my.televip.MainHook through assets/xposed_init.
          Modern frameworks (LSPosed 1.10+, Vector 2.2) ignore this block and use
          META-INF/xposed/java_init.list -> com.my.televip.xposed.TeleVipModule instead.
        -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/app_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposedscope" />

    </application>

</manifest>
